---
sidebar_position: 4
sidebar_label: "Software"
hide_table_of_contents: true
hide_title: true
pagination_next : null
pagination_prev : null
title: "Software"
displayed_sidebar: null
---

## Software - ncurses


#### Description
The ncurses (new curses) library is a free software emulation of curses in System V Release 4.0, and more. It uses terminfo format, supports pads and color and multiple highlights and forms characters and function-key mapping, and has all the other SYSV-curses enhancements over BSD curses.

#### Website
https://invisible-island.net/ncurses/ncurses.html

#### Variants installed

1. ncurses 6.1.20180224

    - Compiler: gcc 9.4.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load ncurses/shvlbnh
    ```

2. ncurses 6.1.20180224

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load ncurses/4aqrzjy
    ```

3. ncurses 6.1.20180224

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load ncurses/wj6bzuz
    ```

4. ncurses 6.1.20180224

    - Compiler: gcc 10.3.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load ncurses/evainq4
    ```

5. ncurses 6.2

    - Compiler: gcc 8.5.0
    - Architecture: zen

    Spack Command
    ```bash
    $ spack load ncurses/id3e5lz
    ```

6. ncurses 6.2

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load ncurses/zl4ycaf
    ```

7. ncurses 6.2

    - Compiler: gcc 11.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load ncurses/5wqakny
    ```

8. ncurses 6.2

    - Compiler: aocc 2.3.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load ncurses/vlpstcv
    ```

9. ncurses 6.2

    - Compiler: gcc 9.3.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load ncurses/4o477z3
    ```

10. ncurses 6.2

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load ncurses/hznv2js
    ```
