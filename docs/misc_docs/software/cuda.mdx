---
sidebar_position: 4
sidebar_label: "Software"
hide_table_of_contents: true
hide_title: true
pagination_next : null
pagination_prev : null
title: "Software"
displayed_sidebar: null
---

## Software - cuda


#### Description
CUDA is a parallel computing platform and programming model invented by NVIDIA. It enables dramatic increases in computing performance by harnessing the power of the graphics processing unit (GPU). Note: This package does not currently install the drivers necessary to run CUDA. These will need to be installed manually. See: https://docs.nvidia.com/cuda/ for details.

#### Website
https://developer.nvidia.com/cuda-zone

#### Variants installed

1. cuda 10.0.130

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/2safcle
    ```

2. cuda 10.2.89

    - Compiler: gcc 8.5.0
    - Architecture: zen

    Spack Command
    ```bash
    $ spack load cuda/4vwpjf2
    ```

3. cuda 11.0.2

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/3wlbq6u
    ```

4. cuda 11.1.1

    - Compiler: aocc 2.3.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/z47kekb
    ```

5. cuda 11.1.1

    - Compiler: gcc 9.3.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/cu2kblh
    ```

6. cuda 11.1.1

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/hmrghph
    ```

7. cuda 11.2.0

    - Compiler: gcc 9.3.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/t4nrkr4
    ```

8. cuda 11.2.0

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/yyzdqac
    ```

9. cuda 11.2.1

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/xznwrmv
    ```

10. cuda 11.2.2

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/ehwaupl
    ```

11. cuda 11.2.2

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/ksolshp
    ```

12. cuda 11.3.0

    - Compiler: gcc 10.2.0
    - Architecture: zen2

    Spack Command
    ```bash
    $ spack load cuda/4p44avb
    ```
